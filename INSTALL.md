# CHB v0.5.4E-F

Copy all files from this package into the repository root and allow replacements.

Run:

```bash
npm run docs:validate
npm run docs:generate
npm run docs:check
npm run release:prepare
```

New generated AI adapters:

- `AGENTS.md`
- `CLAUDE.md`
- `.cursor/rules/chb-project.mdc`
- `docs/AI_PROJECT_INTERFACE.json`
- `docs/ai/PROJECT_INTERFACE.md`

Do not edit generated adapters manually. Edit `docs/data` and regenerate.
