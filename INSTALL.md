# CHB v0.5.4A-D

Skopiuj zawartość paczki do głównego katalogu repozytorium `my-dashboard`.

```bash
npm run docs:validate
npm run docs:generate
npm run docs:check
npm run release:prepare
```

Źródłem prawdy są pliki `docs/data/*.json`. Nie edytuj ręcznie generowanych plików Markdown.
